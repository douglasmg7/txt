# pug 
the new name for jade tamplate

# install pug
npm install  pug

pug --client --no-debug --name template_1 allnations_template.pug 
<script src="js/allnations_template.js"></script>
template: template_1 (into backbone view, template_1 is the name of function in generated js file)

# install pug cli
npm install -g pug-cli

# compile current directory with prettify option
pug -P .

# watch current directory, compile to parent directory and prettify 
pug . -Pwo ..





