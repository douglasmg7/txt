# Everything in python is a object.

# For built-in data structures.
  list
  tuple
  dictionary (map, hash)
  set

# list
  ordered
  mutable
  collection of objects

# tuple
  ordered
  immutable
  collection of objects

# dictionary
  unordered
  set of key/value pairs

# set
  unordered
  set of unique objects

# docstring, used on function documentation.
""" Function do some thing """

# Function.
def do_some_thing(word):
  """ Some documentation """
  for ch in word:
    print(ch)
  return 'fim'
  

# Anotation.
# Not used by compile, just for programming and help bif.
def do_some_thing(word:str) -> str:
  """ Some documentation """
  for ch in word:
    print(ch)
  return 'fim'

