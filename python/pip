# pip (Package insaller for python)
# Uses the Python Index Location pypi.org

# Install a module with pip.
$ pip install package_name
$ pip install package_name==1.4
$ pip install package_name<=1.4
$ pip install package_name>1.4

# Packages in use.
pip freeze > requirements.txt
pip install -r requirements.txt
