# docker-compose

# install
sudo pacman -Syu docker-compose

# Start
docker-compose up
docker-compose up -d    # start not attached
docker-compose up --build    # force rebukld images

# start only one service from compose yml file
docker-compose up $service_name

# stop
docker-compose down
docker-compose down -v  # remove volumes


