!name_variable
impact the current data

poem.lines.to_a.reverse.join
chaining

books = {}
hash / dictionary

books["Key"] = :value
include a value

:splendid
not a string, a ruby symbol

books.keys
list of keys

ratings = Hash.new(0)
build a empty Hash

books.values.each {|rate| ratings[rate] += 1}
populate the ratings rash

5.times { print "teste"}
run 5 times the block

Dir.entries "/"
list /

Dir[/Home/*.txt]
list files

File.read("/comics.txt")
read a file

FileUtils.cp('/file1', '/file2')
copy a file
