# verify
os.tmpDir() use os.tmpdir() instead

# config files
~/.atom/*.cson
~/.atom/*.coffee
~/.atom/*.less
~/.atom/*.json
apm list --installed --bare > ~/.atom/package.list    # save installed packages
apm install --packages-file ~/.atom/package.list      # install saved package list

zen package
	shift-f11
	distraction free

ctrl+shift+d
duplicate line

ctrl+d
select a word

alt + shift + s
see all available snippets for the file type currently open

ctrl + m
go to match bracket

ctrl + alt + m
select text inside bracket

ctrl + p
open a file

ctrl + shift + p
smart command

ctrl + shift + l
show / select grammar

alt + shift + w
suround with tag

ctrl-shift-alt-s
	run shell command on selection (shell-it)
