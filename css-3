border-radius: 4px 15px 12px 10px;

box-shadow: <inset> <offset-x> <offset-y> <blur-radius> <spread-radius> <color>

text-shadow: <offset-x> <offset-y> <blur-radius> <color>

box-sizing: padding-box | border-box

{
  background-mage: url(imga), url(imgb);
  background-position: top left, center right;
  background-repeat: no-repeat, no-repeat;
}
{
  background:
    url(imga) top left no-repeat,
    url(imgb) center right no-repeat
}

background: linear-gradient(to bottom, red, yellow)
background: radial-gradient(aqua, blue)
background: radial-gradient(<shape> <size> at <position>, <color-stop>s)

opacity: .35;

@font-face {
  font-familiy: 'OpenSansRegular';
  src: url('OpenSansRegular-webfont.eot');
  font-style: normal;
  font-weight: normal;
}
h1 {
  font-family: 'OpenSansRegular', Helvetica, Arial, sans-serif;
}

transform: translate(20px, 30px);
transform: translateX(20px);
transform: rotate(45deg);
transform: scale(1.5);
transform: scaleX(3);
transform: skewX(-25deg)

{
  background-color: white;
  transition: background-color: black 0.2s ease-in-out
}
transition: <property> <duration> <timing-function> <delay>
<timing-function> ease | ease-in | ease-in-out | linear

float: left | right | none
removes elements from the document flow and moves them to a specified edge

clear: right | left | both
make the next item not participate from the float element
if floated item is taller than non-floated content
if all children are floating

priority
p {color: #fff !important;}
<h1 style="color: #fff;">Mogul</h1>
#header {color :#fff;}
.intro {color: #fff;}
p {color: #fff;}

overflow: visible | hidden | scroll | auto | hidden
overflow-x:
study more

position: static | relative | absolute | fixed
{
  position: relative;
  top: -0.5em;
}
study more

dry - don't repeat yourself

display: inline-block
same flow as inline element but behave as a block element

margin: 0 auto;
center a block element

text-align: center;
center a inline element;

collapsing margins will not occur when one or more block element has:
padding or border
relative or absolute position
a float left or right

content image should be marked up as inline images
layout elements should be defined as background images

overflow crop method - image crop
li {
  height: 300px;
  width: 400px;
  overflow: hidden;
}

overflow crop method - set just height
.store li {
  height: 150px;
  width: 150px;
  overflow: hidden;
}
.store li img {
  height: 150px;
    width: auto;
}