# edit command line on vim
<C-x>,<C-e>

cat /etc/shells		# list available shells

echo $SHELL   # show current shell in use

zsh --version   # show version

chsh -s $(which zsh)  # change default shell to zsh
