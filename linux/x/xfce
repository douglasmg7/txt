/etc/xdg/menus/xfce-applications.menu
~/.config/menus
menu configuration

*.desktop
/usr/share/applications
~/.local/share/applications
files for menu entry

icons
/usr/share/icons

xfce4-terminal --hide-menubar --hide-toolbar --hide-borders

/etc/lightdm/lightdm.conf
autologin-user=
autologin-user-timeout=

startx
startxfce4
start gui

lightdm (xfce desktop manager)
kdm (kde desktop manager)
gdm (gnome desktop manager)

alt+rmb
# resize window

alt+f7
# move window

alt+f9
# hide window

shift+alt-page_down
# switch window

ctrl+alt+d
# shoe desktop

xfce4-mixer
sound volume

# open a file
xdg-open file_name

# no min max buttons
cd ~/.cache
rm -rf sessions
xfwm4 --replace
