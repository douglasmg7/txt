ffmpeg -i walk-downtown.mp4 -vn -acodec libmp3lame -ac 2 -ab 160k -ar 48000 walk-downtown.mp3   
# convert mp4 to mp3

ffmpeg -i example.mkv -c copy -an example-nosound.mkv
# remove audio from video.
