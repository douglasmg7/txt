# Run script on same process as the calling script.
. prog.sh
. ./prog.sh
source ./prog.sh

# Exit script without close shell.
return

# Command substitution, backtick or $().
# $() nests better.
echo `id -u`